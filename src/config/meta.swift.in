let commit = "@COMMIT@"
// swift-format-ignore
let unixTime = @UNIX_TIME@
let recentTag = "@RECENT_TAG@"
let recentCommits = @RECENT_COMMITS@
let releaseTag = recentCommits == 0 ? recentTag : "latest"
